'use client'

import React from "react";
import {NodeData} from "@/model/NodeData";
import {LoadingButton} from "@mui/lab";
import PlayCircleFilledIcon from '@mui/icons-material/PlayCircleFilled';
import {usePlayStore} from "@/stores/editor/PlayStore";
import {
    createNodeComponent,
    createNodeShapeStyle,
    createOptionsComponent
} from "@/components/editor/pages/canvas/nodes/util/Creators";
import {NodeTypes} from "@/model/NodeTypes";
import PlayArrowIcon from "@mui/icons-material/PlayArrow";

// --- Data ---
export interface [TODO] extends NodeData {
    // TODO Add Data Attributes here
}


// --- Style ---
export const [TODO]ShapeStyle = createNodeShapeStyle({
    // TODO Add additional CSS for the nodes shape here
})


// --- Node ---
const [TODO]Node = createNodeComponent<[TODO]NodeData>(
    NodeTypes.[TODO]_NODE,
    [TODO]ShapeStyle,
    (id, selected, data) => {
        // TODO Place the node content here
    }
)

export default StartNode

// --- Options ---
export const [TODO]Options = createOptionsComponent<[TODO]NodeData>("Start", ({ id, data, onDataUpdated }) => {
    return // TODO Place options here
})